#include "CircleHitbox.h"
#include "RectangularHitbox.h"
#include "tests.h"
#include "Hitbox.h"

int main(){

	/*for (int it = 0; it < 11; it++) {
	   std::cout << RectangularHitboxesIntersectionTest(it);
	}

	for (int it = 0; it < 8; it++) {
		std::cout << CircleHitboxesIntercectionTest(it);
	}

	for (int it = 0; it < 12; it++) {
		std::cout << CircleAndRectangularHitboxesIntersectionTest(it);
	}*/

	/*RectangularHitbox* a = new RectangularHitbox[3];
	Point c, d;
	c.setCoordinates(-1, 2);
	d.setCoordinates(1, 4);
	a[0].setPoints(c, d);
	c.setCoordinates(-5, -1);
	d.setCoordinates(-2, 1);
	a[1].setPoints(c, d);
	c.setCoordinates(2, -1);
	d.setCoordinates(5, 1);
	a[2].setPoints(c, d);
	CircleHitbox* b = new CircleHitbox[1];
	b[0].setRadius(2);
	Hitbox A;
	A.setHitbox(1, b, 3, a);

	RectangularHitbox* a1 = new RectangularHitbox[1];
	c.setCoordinates(-3, 0);
	d.setCoordinates(3, 3);
	a1[0].setPoints(c, d);
	CircleHitbox* b1 = new CircleHitbox[1];
	b1[0].setRadius(2);
	Hitbox B;
	B.setHitbox(1, b1, 1, a1);
	B.setBasePoint(8, 0);

	std::cout << A.checkForCollision(B);

	return 0;*/

}